alert("working")
/*async function allTodos(){
	const allTodos = await fetch('http://localhost:3000/home/alltodos')
	const data = await res.json()
	alert(data)
}
allTodos()*/
/*const checkBox = document.querySelector("#task1")
checkBox.addEventListener("change",(e)=>{
    const task = document.querySelector("#text1")
    const status = document.querySelector('#status1')
  if(e.target.checked){
    task.classList.add('complete')
    status.innerText= "complete"
  }
  else{
    task.classList.remove('complete')
    task.classList.add('incomplete')
    status.innerText="incomplete"
  }
})*/
